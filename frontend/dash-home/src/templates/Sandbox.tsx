import * as React from 'react';

interface Props {}
interface State {}

class Sandbox extends React.Component<Props, State> {
  render() {
    return (
      <div>
        <h1>Sandbox</h1>
        {/* Write something... */}
      </div>
    );
  }
};

export default Sandbox;